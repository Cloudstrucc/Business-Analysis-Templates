<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}} | Cloudstrucc BA Forms</title>
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  
  <style>
    :root {
      /* Exact diagrams.cloudstrucc.com colors */
      --slate-950: #020617;
      --slate-900: #0f172a;
      --slate-800: #1e293b;
      --slate-700: #334155;
      --slate-600: #475569;
      --slate-500: #64748b;
      --slate-400: #94a3b8;
      --slate-300: #cbd5e1;
      --slate-200: #e2e8f0;
      --slate-100: #f1f5f9;
      --slate-50: #f8fafc;
      --white: #ffffff;
      --cyan-400: #22d3ee;
      --cyan-500: #06b6d4;
      --cyan-600: #0891b2;
      --blue-500: #3b82f6;
      --blue-600: #2563eb;
      --emerald-500: #10b981;
      --amber-500: #f59e0b;
      --red-500: #ef4444;
    }
    
    * { box-sizing: border-box; }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background: var(--slate-900);
      color: var(--white);
      margin: 0;
      min-height: 100vh;
    }
    
    a { text-decoration: none; }
    
    /* ============ ADMIN LAYOUT ============ */
    .admin-layout {
      display: flex;
      min-height: 100vh;
    }
    
    /* Sidebar */
    .sidebar {
      width: 260px;
      background: var(--slate-950);
      border-right: 1px solid var(--slate-800);
      position: fixed;
      height: 100vh;
      display: flex;
      flex-direction: column;
    }
    
    .sidebar-header {
      padding: 1.5rem;
      border-bottom: 1px solid var(--slate-800);
    }
    
    .sidebar-header a {
      color: var(--white);
      font-weight: 600;
      font-size: 1.125rem;
      display: flex;
      align-items: center;
      gap: 0.625rem;
    }
    
    .sidebar-header i {
      color: var(--cyan-400);
      font-size: 1.25rem;
    }
    
    .sidebar-nav {
      flex: 1;
      padding: 1.5rem 0;
      overflow-y: auto;
    }
    
    .nav-group-label {
      padding: 0.5rem 1.5rem;
      font-size: 0.6875rem;
      font-weight: 600;
      color: var(--slate-500);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }
    
    .nav-item {
      display: block;
      padding: 0.75rem 1.5rem;
      color: var(--slate-400);
      font-size: 0.875rem;
      font-weight: 500;
      transition: all 0.15s;
      border-left: 2px solid transparent;
    }
    
    .nav-item:hover {
      color: var(--white);
      background: var(--slate-800);
    }
    
    .nav-item.active {
      color: var(--white);
      background: var(--slate-800);
      border-left-color: var(--cyan-400);
    }
    
    .nav-item i {
      margin-right: 0.75rem;
      width: 18px;
      display: inline-block;
    }
    
    .sidebar-footer {
      padding: 1rem 1.5rem;
      border-top: 1px solid var(--slate-800);
    }
    
    .user-badge {
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }
    
    .user-avatar {
      width: 36px;
      height: 36px;
      background: var(--cyan-500);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 600;
      font-size: 0.875rem;
    }
    
    .user-name {
      font-size: 0.875rem;
      font-weight: 500;
    }
    
    .user-role {
      font-size: 0.75rem;
      color: var(--slate-500);
    }
    
    /* Main Content */
    .main-wrap {
      flex: 1;
      margin-left: 260px;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    
    .topbar {
      padding: 1rem 2rem;
      border-bottom: 1px solid var(--slate-800);
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: var(--slate-900);
      position: sticky;
      top: 0;
      z-index: 50;
    }
    
    .topbar h1 {
      font-size: 1.25rem;
      font-weight: 600;
      margin: 0;
    }
    
    .topbar-actions .btn {
      background: transparent;
      border: 1px solid var(--slate-700);
      color: var(--slate-300);
      padding: 0.5rem 1rem;
      border-radius: 8px;
      font-size: 0.875rem;
      font-weight: 500;
      transition: all 0.15s;
    }
    
    .topbar-actions .btn:hover {
      border-color: var(--cyan-400);
      color: var(--cyan-400);
    }
    
    .page-content {
      flex: 1;
      padding: 2rem;
    }
    
    /* Cards */
    .card {
      background: var(--slate-800);
      border: 1px solid var(--slate-700);
      border-radius: 12px;
    }
    
    .card-header {
      padding: 1.25rem 1.5rem;
      border-bottom: 1px solid var(--slate-700);
      background: transparent;
      font-weight: 600;
      font-size: 0.9375rem;
    }
    
    .card-body {
      padding: 1.5rem;
    }
    
    /* Stat Cards */
    .stat-card {
      background: var(--slate-800);
      border: 1px solid var(--slate-700);
      border-radius: 12px;
      padding: 1.5rem;
    }
    
    .stat-card .stat-icon {
      width: 48px;
      height: 48px;
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.25rem;
      margin-bottom: 1rem;
    }
    
    .stat-card .stat-icon.cyan { background: rgba(6,182,212,0.15); color: var(--cyan-400); }
    .stat-card .stat-icon.blue { background: rgba(59,130,246,0.15); color: var(--blue-500); }
    .stat-card .stat-icon.emerald { background: rgba(16,185,129,0.15); color: var(--emerald-500); }
    .stat-card .stat-icon.amber { background: rgba(245,158,11,0.15); color: var(--amber-500); }
    
    .stat-card .stat-value {
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: 0.25rem;
    }
    
    .stat-card .stat-label {
      font-size: 0.875rem;
      color: var(--slate-400);
    }
    
    /* Buttons */
    .btn-primary {
      background: var(--cyan-500);
      border: none;
      color: var(--white);
      padding: 0.625rem 1.25rem;
      border-radius: 8px;
      font-weight: 500;
      font-size: 0.875rem;
      transition: all 0.15s;
    }
    
    .btn-primary:hover {
      background: var(--cyan-600);
      color: var(--white);
    }
    
    .btn-secondary {
      background: var(--slate-700);
      border: none;
      color: var(--white);
      padding: 0.625rem 1.25rem;
      border-radius: 8px;
      font-weight: 500;
    }
    
    .btn-secondary:hover {
      background: var(--slate-600);
      color: var(--white);
    }
    
    .btn-outline {
      background: transparent;
      border: 1px solid var(--slate-600);
      color: var(--slate-300);
      padding: 0.625rem 1.25rem;
      border-radius: 8px;
      font-weight: 500;
    }
    
    .btn-outline:hover {
      border-color: var(--slate-500);
      color: var(--white);
    }
    
    /* Tables */
    .table {
      color: var(--slate-300);
      margin: 0;
    }
    
    .table thead th {
      background: var(--slate-900);
      color: var(--slate-400);
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      padding: 1rem 1.25rem;
      border-bottom: 1px solid var(--slate-700);
      border-top: none;
    }
    
    .table tbody td {
      padding: 1rem 1.25rem;
      border-color: var(--slate-700);
      vertical-align: middle;
    }
    
    .table tbody tr:hover {
      background: var(--slate-800);
    }
    
    /* Badges */
    .badge {
      padding: 0.375rem 0.75rem;
      border-radius: 6px;
      font-size: 0.75rem;
      font-weight: 500;
    }
    
    .badge-cyan { background: rgba(6,182,212,0.15); color: var(--cyan-400); }
    .badge-emerald { background: rgba(16,185,129,0.15); color: var(--emerald-500); }
    .badge-amber { background: rgba(245,158,11,0.15); color: var(--amber-500); }
    .badge-red { background: rgba(239,68,68,0.15); color: var(--red-500); }
    .badge-slate { background: var(--slate-700); color: var(--slate-300); }
    
    /* Forms */
    .form-label {
      font-size: 0.875rem;
      font-weight: 500;
      color: var(--slate-300);
      margin-bottom: 0.5rem;
    }
    
    .form-control, .form-select {
      background: var(--slate-900);
      border: 1px solid var(--slate-700);
      color: var(--white);
      border-radius: 8px;
      padding: 0.625rem 1rem;
      font-size: 0.875rem;
    }
    
    .form-control:focus, .form-select:focus {
      background: var(--slate-900);
      border-color: var(--cyan-500);
      color: var(--white);
      box-shadow: 0 0 0 3px rgba(6,182,212,0.15);
    }
    
    .form-control::placeholder {
      color: var(--slate-500);
    }
    
    /* Alerts */
    .alert {
      border: none;
      border-radius: 10px;
      padding: 1rem 1.25rem;
      font-size: 0.875rem;
    }
    
    .alert-success {
      background: rgba(16,185,129,0.15);
      color: var(--emerald-500);
    }
    
    .alert-danger {
      background: rgba(239,68,68,0.15);
      color: var(--red-500);
    }
    
    .alert-info {
      background: rgba(6,182,212,0.15);
      color: var(--cyan-400);
    }
    
    /* Progress */
    .progress {
      height: 6px;
      background: var(--slate-700);
      border-radius: 3px;
    }
    
    .progress-bar {
      background: var(--cyan-500);
    }
    
    /* Modals */
    .modal-content {
      background: var(--slate-800);
      border: 1px solid var(--slate-700);
      border-radius: 16px;
    }
    
    .modal-header {
      border-bottom: 1px solid var(--slate-700);
      padding: 1.25rem 1.5rem;
    }
    
    .modal-header .btn-close {
      filter: invert(1);
    }
    
    .modal-body {
      padding: 1.5rem;
    }
    
    .modal-footer {
      border-top: 1px solid var(--slate-700);
      padding: 1rem 1.5rem;
    }
    
    /* Empty State */
    .empty-state {
      text-align: center;
      padding: 4rem 2rem;
      color: var(--slate-500);
    }
    
    .empty-state i {
      font-size: 3rem;
      margin-bottom: 1rem;
      color: var(--slate-600);
    }
    
    .empty-state h5 {
      color: var(--slate-300);
      margin-bottom: 0.5rem;
    }
    
    /* ============ PUBLIC LAYOUT ============ */
    .public-layout {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }
    
    .public-header {
      padding: 1.25rem 0;
      border-bottom: 1px solid var(--slate-800);
    }
    
    .public-header .container {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    
    .public-header .brand {
      color: var(--white);
      font-weight: 600;
      font-size: 1.125rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    
    .public-header .brand i {
      color: var(--cyan-400);
    }
    
    .public-header .nav-links {
      display: flex;
      align-items: center;
      gap: 2rem;
    }
    
    .public-header .nav-links a {
      color: var(--slate-400);
      font-size: 0.9rem;
      font-weight: 500;
      transition: color 0.15s;
    }
    
    .public-header .nav-links a:hover {
      color: var(--white);
    }
    
    .public-header .nav-links .btn {
      background: var(--cyan-500);
      color: var(--white);
      padding: 0.5rem 1rem;
      border-radius: 8px;
      font-weight: 500;
    }
    
    .public-header .nav-links .btn:hover {
      background: var(--cyan-600);
    }
    
    .public-main {
      flex: 1;
    }
    
    .public-footer {
      border-top: 1px solid var(--slate-800);
      padding: 2rem 0;
      text-align: center;
    }
    
    .public-footer p {
      color: var(--slate-500);
      font-size: 0.875rem;
      margin: 0;
    }
    
    .public-footer a {
      color: var(--cyan-400);
    }
    
    /* Responsive */
    @media (max-width: 991px) {
      .sidebar { transform: translateX(-100%); transition: transform 0.2s; position: fixed; z-index: 1000; }
      .sidebar.show { transform: translateX(0); }
      .main-wrap { margin-left: 0; }
      .mobile-toggle { display: inline-flex !important; }
    }
    
    .mobile-toggle { display: none; }
  </style>
</head>
<body>
  {{#if isAdmin}}
  <div class="admin-layout">
    <aside class="sidebar" id="sidebar">
      <div class="sidebar-header">
        <a href="/admin/dashboard">
          <i class="bi bi-diagram-3"></i> Cloudstrucc
        </a>
      </div>
      
      <nav class="sidebar-nav">
        <div class="nav-group-label">Main</div>
        <a href="/admin/dashboard" class="nav-item {{#if isDashboard}}active{{/if}}">
          <i class="bi bi-grid-1x2"></i>Dashboard
        </a>
        
        <div class="nav-group-label">Management</div>
        <a href="/admin/invites" class="nav-item {{#if isInvites}}active{{/if}}">
          <i class="bi bi-envelope"></i>Invites
        </a>
        <a href="/admin/submissions" class="nav-item {{#if isSubmissions}}active{{/if}}">
          <i class="bi bi-inbox"></i>Submissions
        </a>
        <a href="/admin/forms" class="nav-item {{#if isForms}}active{{/if}}">
          <i class="bi bi-file-text"></i>Forms
        </a>
        
        <div class="nav-group-label">Account</div>
        <a href="/admin/logout" class="nav-item">
          <i class="bi bi-box-arrow-left"></i>Sign Out
        </a>
      </nav>
      
      <div class="sidebar-footer">
        <div class="user-badge">
          <div class="user-avatar">{{#if admin}}{{substring admin.email 0 1}}{{else}}A{{/if}}</div>
          <div>
            <div class="user-name">{{#if admin}}{{admin.email}}{{else}}Admin{{/if}}</div>
            <div class="user-role">Administrator</div>
          </div>
        </div>
      </div>
    </aside>
    
    <div class="main-wrap">
      <header class="topbar">
        <div class="d-flex align-items-center gap-3">
          <button class="btn mobile-toggle d-lg-none" onclick="document.getElementById('sidebar').classList.toggle('show')">
            <i class="bi bi-list"></i>
          </button>
          <h1>{{title}}</h1>
        </div>
        <div class="topbar-actions">
          <a href="/" class="btn" target="_blank">
            <i class="bi bi-box-arrow-up-right me-2"></i>View Site
          </a>
        </div>
      </header>
      
      <main class="page-content">
        {{#if messages.success}}
        <div class="alert alert-success mb-4">
          <i class="bi bi-check-circle me-2"></i>{{messages.success}}
        </div>
        {{/if}}
        
        {{#if messages.error}}
        <div class="alert alert-danger mb-4">
          <i class="bi bi-exclamation-circle me-2"></i>{{messages.error}}
        </div>
        {{/if}}
        
        {{{body}}}
      </main>
    </div>
  </div>
  {{else}}
  <div class="public-layout">
    <header class="public-header">
      <div class="container">
        <a href="/" class="brand">
          <i class="bi bi-diagram-3"></i> Cloudstrucc
        </a>
        <nav class="nav-links">
          <a href="https://cloudstrucc.com">About</a>
          <a href="https://diagrams.cloudstrucc.com">Diagrams</a>
          <a href="/admin/login" class="btn">Sign In</a>
        </nav>
      </div>
    </header>
    
    <main class="public-main">
      {{{body}}}
    </main>
    
    <footer class="public-footer">
      <div class="container">
        <p>&copy; {{currentYear}} <a href="https://cloudstrucc.com">Cloudstrucc Inc.</a> All Rights Reserved.</p>
      </div>
    </footer>
  </div>
  {{/if}}
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
